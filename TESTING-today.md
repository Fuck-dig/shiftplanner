# Moved

This was the test pass for the 3 Aug changes. It has been folded into
`TESTING.md` — see **section 10**, which carries the two parts that were never
run (the rebuilt staff Requests tab and the manager approvals queue), plus the
archive-with-OK path.

Sections A and C were completed on 4 Aug; what they found is recorded in
`CHANGELOG.md`.

Kept as a pointer rather than deleted so nothing links into a missing file.
Safe to delete whenever you like — I can't remove it from here.
