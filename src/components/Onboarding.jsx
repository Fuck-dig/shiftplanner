// Dead file — imported in App.jsx historically but never rendered; the
// "create your first restaurant" flow is handled by RestaurantPicker.jsx
// (orgs.length===0 branch) instead. Left as a placeholder because files in
// this folder can't be deleted from this environment; safe to delete manually.
