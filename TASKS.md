# Tasks

<!-- Severity scale (1-10): how worried to be if this is left as-is.
     1-3 = cosmetic/nice-to-have · 4-6 = real problem, not urgent
     7-8 = should fix soon · 9-10 = data loss, security, or breakage. -->

## Active

- [ ] **Run the 4 pending Supabase migrations on the live project** - severity 8/10 - the app code for org currency, manager-only documents, the split-out wages table, and open shifts is all written and will misbehave against the old schema until these run: 20260728120000_org_currency.sql, 20260728130000_documents_manager_only_rls.sql, 20260728140000_employee_wages.sql (run in one go), 20260803120000_open_shifts.sql
- [ ] **Test overtime/rest warnings, employee documents, and push notifications on rorota.net** - severity 5/10 - all three shipped and deployed (commit d56063c); overtime/rest warnings confirmed working in the edit-shift modal, documents upload/open/remove confirmed working, push subscribe + toggles confirmed working — still need to confirm actual push delivery fires for: new/changed shifts (confirm schedule), time-off/swap decisions, direct messages, and shift reminders (via the send-shift-reminders cron or a manual curl trigger)
- [ ] **Set up a staging environment / less manual deploy flow** - severity 4/10 - from CRITIQUE.md - every change currently goes straight from local edits to production via a manual git push, with no gate in between

## Waiting On

## Someday

- [ ] **Onboarding walkthrough / tutorial** - severity 3/10 - William's idea, explicitly deferred - a guided first-run tour once someone signs up, not scoped or started yet
- [ ] **Archive instead of delete for departed staff** - severity 4/10 - deleting an employee now also removes their shift history (see the orphaned-shifts fix below). Fine for a mis-added row, wrong if you ever need past hours for payroll or a dispute. Would mean an `archived` flag that hides someone from scheduling while keeping their history intact.

## Done

- [x] ~~Open shifts on every cell + drag-and-drop to move people~~ (2026-08-03) - severity 3/10 - "Post as open shift" used to only appear on a slot that was already short-staffed; it's now on every cell (short label "+ Open" in the common fully-staffed case, full wording as the tooltip), since a fully-staffed day might still want an extra pair of hands and there was no reason posting one should require first creating a gap. Drag-and-drop: drag anyone in the Week grid onto empty space in another cell to move them there, or onto another person to swap the two - whoever lands in a slot takes on that slot's role, matching the existing click-to-move behaviour, which still works alongside it. The move/swap maths is extracted into applyAssignmentDrop in lib/schedule.js (8 new tests, 87 total) rather than living in App.jsx, because a bug there duplicates or silently drops somebody's shift - the tests assert headcount is preserved across every move, custom times and clocked data survive the move, and a stale index returns null instead of corrupting the schedule.

- [x] ~~Fix production white-screen ("Der gik noget galt") from a rules-of-hooks violation~~ (2026-08-03) - severity 9/10 - MY BUG, shipped in the open-shifts commit. I added a `useCallback` for openShiftsFor at line ~1041 of App.jsx, which sits below the `if(loading) return <LoadingScreen/>` early return at line 441. That means the loading render ran fewer hooks than the loaded render, React threw "rendered more hooks than during the previous render", and the ErrorBoundary caught it - so every user got the error screen instead of the app. Fixed by making openShiftsFor a plain function (filtering a short array per cell is cheap; it never needed memoizing). Prevention: eslint.config.js already had react-hooks/rules-of-hooks configured, but eslint was never actually installed and there was no lint script, so the config had never run. Installed eslint + plugins, added `npm run lint` (full, ~140 mostly-cosmetic pre-existing issues) and `npm run lint:ci` (narrow gate, hooks rules only), and wired lint:ci into CI ahead of test/build. Verified by reintroducing the exact bug: lint:ci fails on it and passes once fixed.

- [x] ~~Add an error boundary~~ (2026-07-28) - severity 6/10 - wrapped the app root in a top-level ErrorBoundary so an uncaught render error shows a "something went wrong, reload" screen instead of blanking to white
- [x] ~~Give brand-new orgs an empty state instead of fake employees~~ (2026-07-28) - severity 7/10 - removed the DEFAULT_EMPLOYEES fallback entirely (kept the default Lunch/Dinner coverage blocks as a starter template, since that's a generic template rather than fake personal data); EmployeesView now shows a real "no employees yet" empty state. This only changes what happens when an org's employee list is genuinely empty, so it has zero effect on any org — including Almus — that already has real employees in it.
- [x] ~~Ask for currency when creating a new restaurant~~ (2026-07-28) - severity 5/10 - currency is now a real per-org database setting (organizations.currency, defaulting to 'kr' so every existing org's behavior is unchanged) instead of a single browser-wide localStorage value shared across all restaurants; the "create restaurant" form now asks for it, and changing it later in Costs saves back to that org.
- [x] ~~Code-split the JS bundle~~ (2026-07-28) - severity 4/10 - lazy-loaded EmployeeView, KioskView, and the two message modals (each only needed for a subset of sessions); single ~800kB chunk is now a ~375kB main chunk plus several small on-demand ones, and the "chunk >500kB" build warning is gone
- [x] ~~Fix RLS on employee_documents and wages~~ (2026-07-28) - severity 9/10 - documents: tightened all 6 policies (table + storage) to require an owner/manager membership role, not just org membership. Wages: found this was worse than expected — EmployeeView already downloaded every coworker's wage on ordinary use, just never displayed it, and RLS can't fix that alone since it filters rows not columns. Moved wage/contract_type/contract_period into a new employee_wages table with manager-only RLS (zero policies for a plain employee), backfilled from the existing employees table before dropping those columns from it. Two new migrations to run: 20260728130000_documents_manager_only_rls.sql and 20260728140000_employee_wages.sql (run the second one in one go — it copies data before dropping the old columns).
- [x] ~~Add real test coverage beyond schedule.js + set up CI~~ (2026-08-03) - severity 5/10 - added dates.test.js (14 tests, incl. Sunday edge cases for getMondayDate/weekOffsetFromDate via fake timers), roles.test.js (10 tests for drag-reorder + merge logic), and html.test.js (6 tests for escapeHtml) - test count went from 43 to 73, all passing. Added .github/workflows/ci.yml which runs npm test + npm run build automatically on every push/PR to main - so a broken build or failing test now gets caught before it reaches production instead of only being noticed manually. (lib/data.js and lib/org.js still untested - they need Supabase client mocking, left for a future pass.)
- [x] ~~Fix Team tab vs Week tab showing different shifts for the same week~~ (2026-08-03) - severity 7/10 - root cause: deleting an employee never removed their existing shift assignments from the schedule, leaving them as orphans pointing at an id that no longer exists. The Team grid only matches assignments against current employees, so it silently dropped them ("0 scheduled" despite real shifts that day); the Week grid falls back to the assignment's frozen name and kept showing it, but with a generic fallback color instead of that person's real one — the two symptoms you spotted (missing shifts + everyone showing the same blue) were the same bug. Added pruneOrphanedAssignments (lib/schedule.js, 6 new tests) - removeEmp now strips a deleted employee's shifts from every week immediately, and a one-time cleanup runs on load to fix any already-orphaned shifts left over from before this fix existed (Almus's data included). Note: this means deleting someone now also removes their past shift history, not just future shifts - flag if you'd rather keep historical records for departed staff, that'd be a separate "archive instead of delete" feature.
- [x] ~~Condense the Schedule page header + fix an untranslated tooltip~~ (2026-08-03) - severity 3/10 - the Team tab used to stack two separate sticky toolbars (date nav/Today/Week-Month-Team tabs, then a second bar for By name/By role/Compact/Search/count) before you reached any actual schedule. Merged the second bar into the first as one flex-wrap row, so on a normal-width screen everything sits side by side on one line instead of two stacked bars; also trimmed the padding on the leave/coverage-notes/warning banners underneath. Also fixed a real bug spotted in your screenshot: the "+" add-shift button's tooltip was showing the raw key "grid.addShiftTitle" instead of real text, in all 5 languages — that translation key was simply never added. Follow-up: the Team grid's sticky header now measures the real height of the bar above it (ResizeObserver) instead of a hardcoded guess, and removed a leftover negative-margin trick that was bleeding the header's background into the warning banner above it.
- [x] ~~Day-isolated Gantt view: taller bars + Compact/Comfortable toggle; hover-highlight day columns in the Week grid; logo as home button~~ (2026-08-03) - severity 2/10 - the single-day timeline's bars/labels are now bigger by default (34px rows, 13px time labels, up from 24px/10px) since the old size was the smallest text on the page; added the same Compact/Comfortable toggle the Team tab already had next to By role/By name there, so you can flip back to the tighter size — it's one shared setting between both views. In the normal (non-isolated) Week grid, hovering anywhere in a day's column now highlights that whole column (header included), so the grid reads more like separate day cards side by side. The "Rorota" wordmark top-left is now a home button — takes you back to the Schedule tab with nothing isolated, doesn't change which week you're on.
- [x] ~~Add open shifts (manager posts an unfilled slot, staff claim it)~~ (2026-08-03) - severity 5/10 - an unfilled slot in the Week grid now has a "Post as open shift" button next to the "-N short" marker. Posting it notifies every eligible employee (right role, not on leave, not already on that block — same test their own app uses, so nobody gets pinged about something they can't take), they claim it from their app, and it lands in your existing swap-approval queue. Deliberately reuses the shift_swaps table rather than a parallel system: an open shift is just a swap with no original owner, so claiming, approving, and the notification flow all worked already. The one real difference is that approving ADDS the claimant rather than swapping them in for someone, which needed a branch in approveSwap. New migration 20260803120000_open_shifts.sql (drops the NOT NULL on shift_swaps.from_emp_id — safe, existing rows all have one). Also guarded both notify() helpers against a null recipient, since notifications.emp_id is NOT NULL and an open shift has nobody to notify on the "from" side.
- [x] ~~Card-style Week view cells~~ (2026-08-03) - severity 2/10 - each assigned person in the 7-day Week grid is now a full-width card (avatar, name, shift time, and any clocked/no-show status inline) instead of a small pill with loose text hanging underneath. New EmpCard in ui.jsx sitting alongside the existing EmpChip, which is still used everywhere genuinely tight — the on-leave banner and the isolated single-day row, where a full-width card would look wrong.
